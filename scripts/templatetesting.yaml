alias: "Test"
sequence:
  - service_template: >
      {%- if is_state('switch.kitchen_window_switch', 'on') -%}
        switch.turn_off
      {%- else -%}
      {%- endif %}
    entity_id: switch.kitchen_window_switch
